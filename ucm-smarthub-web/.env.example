# URL base da API backend — INCLUI o sufixo /api (usado directamente como
# baseURL do axios). Gravado no bundle em build-time, não é lido em runtime —
# mudar isto exige reconstruir a imagem/bundle do frontend.
VITE_API_URL=http://localhost:5000/api
